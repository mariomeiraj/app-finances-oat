name: app_finances_oat
description: "Aplicativo de controle financeiro pessoal com arquitetura MVVM."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  sqflite: ^2.3.0
  path: ^1.9.0
  intl: ^0.20.2
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
  http: ^1.2.0
  sqflite_common_ffi: ^2.4.0+3
  supabase_flutter: ^2.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
